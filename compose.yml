services:
  claude:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - ${PWD}:/workspace
      - claude-config:/root
    env_file:
      - .env
    stdin_open: true
    tty: true

volumes:
  claude-config:
    driver: local
